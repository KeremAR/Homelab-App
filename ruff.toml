line-length = 88
target-version = "py311"
extend-exclude = [
    ".venv",
    ".venvs",
    "coverage-reports",
    "frontend/node_modules",
]

[lint]
# Preserve Flake8's default pycodestyle and Pyflakes rule set.
select = ["E4", "E7", "E9", "F"]

